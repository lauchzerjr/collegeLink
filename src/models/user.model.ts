export interface UserPostInfos {
  name: string;
  email: string;
  userPhoto: string;
  createdAt: string;
}

export interface UserPostCommentInfos {
  name: string;
  userPhoto: string;
  createdAt: string;
}

export interface UserProfileInfo {
  name: string;
  city: string;
  linkedin: string;
  bio: string;
  userPhoto: string;
  email: string;
  createdAt: string;
}
